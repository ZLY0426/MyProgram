<UserControl x:Class="MyProgram.Views.ModbusRtuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             xmlns:converters="clr-namespace:MyProgram.Convertors"
             d:DesignHeight="800" d:DesignWidth="1200">
    <UserControl.Resources>
        <!-- 工业风样式：简洁、深色边框、清晰的分组 -->
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="#707070"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="5,3"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- 标题 -->
            <RowDefinition Height="Auto"/>
            <!-- 上半部分：配置区 -->
            <RowDefinition Height="*"/>
            <!-- 下半部分：数据区 -->
        </Grid.RowDefinitions>

        <!-- 1. 标题 -->
        <TextBlock 
            Grid.Row="0"
            Text="Modbus RTU 数据采集监控" 
            FontSize="24" 
            FontWeight="Bold" 
            Margin="10,0,0,15"/>

        <!-- 2. 上半部分：配置与控制区 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <!-- 串口配置 -->
                <ColumnDefinition Width="*"/>
                <!-- 采集配置 -->
            </Grid.ColumnDefinitions>

            <!-- 左侧：串口连接配置 -->
            <GroupBox Grid.Column="0" Header="串口连接配置">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 串口号 -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="串口号:" VerticalAlignment="Center" Margin="0,5"/>
                    <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding SerialPorts}" SelectedItem="{Binding SelectedPort}" Margin="5,5" Height="30"/>

                    <!-- 波特率 -->
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="波特率:" VerticalAlignment="Center" Margin="0,5"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding BaudRates}" SelectedItem="{Binding SelectedBaudRate}" Margin="5,5" Height="30"/>

                    <!-- 数据位 -->
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="数据位:" VerticalAlignment="Center" Margin="0,5"/>
                    <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding DataBitsOptions}" SelectedItem="{Binding SelectedDataBits}" Margin="5,5" Height="30"/>

                    <!-- 停止位 -->
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="停止位:" VerticalAlignment="Center" Margin="0,5"/>
                    <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding StopBitsOptions}" SelectedItem="{Binding SelectedStopBits}" Margin="5,5" Height="30"/>

                    <!-- 校验位 -->
                    <TextBlock Grid.Row="4" Grid.Column="0" Text="校验位:" VerticalAlignment="Center" Margin="0,5"/>
                    <ComboBox Grid.Row="4" Grid.Column="1" ItemsSource="{Binding ParityOptions}" SelectedItem="{Binding SelectedParity}" Margin="5,5" Height="30"/>

                    <!-- 右侧：连接控制与状态 -->
                    <StackPanel Grid.Column="2" Margin="10,0,0,0" VerticalAlignment="Center">
                        <!-- 状态指示灯 -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <Ellipse Width="15" Height="15" Fill="{Binding ConnectionStatusColor}" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding ConnectionStatusText}" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>

                        <Button Content="连接" Command="{Binding ConnectCommand}"/>
                        <Button Content="断开" Command="{Binding DisconnectCommand}"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <!-- 右侧：数据采集配置 -->
            <GroupBox Grid.Column="1" Header="数据采集配置">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 从站地址 -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="从站地址:" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SlaveId}" Margin="5,5" Height="30" VerticalContentAlignment="Center"/>

                    <!-- 起始地址 -->
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="起始地址:" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding StartAddress}" Margin="5,5" Height="30" VerticalContentAlignment="Center"/>

                    <!-- 寄存器数量 -->
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="读取数量:" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding RegisterCount}" Margin="5,5" Height="30" VerticalContentAlignment="Center"/>

                    <!-- 写入值 -->
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="写入值:" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding WriteValue}" Margin="5,5" Height="30" VerticalContentAlignment="Center"/>

                    <!-- 采集间隔 -->
                    <TextBlock Grid.Row="4" Grid.Column="0" Text="采集间隔(ms):" VerticalAlignment="Center" Margin="0,5"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding PollingInterval}" Margin="5,5" Height="30" VerticalContentAlignment="Center"/>

                    <!-- 右侧：操作按钮 -->
                    <StackPanel Grid.Column="2" Margin="10,0,0,0" VerticalAlignment="Center">
                        <Button Content="单次读取" Command="{Binding ReadCommand}"/>
                        <Button Content="写入单寄存器" Command="{Binding WriteCommand}"/>
                        <Button Content="{Binding AutoPollingButtonText}" Command="{Binding ToggleAutoPollingCommand}" Background="#FF2196F3" Foreground="White"/>
                    </StackPanel>
                </Grid>
            </GroupBox>
        </Grid>

        <!-- 3. 下半部分：数据展示区 -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <!-- 寄存器数据 -->
                <RowDefinition Height="Auto"/>
                <!-- 通信日志 -->
            </Grid.RowDefinitions>

            <!-- 寄存器数据表格 -->
            <GroupBox Grid.Row="0" Header="实时数据展示">
                <DataGrid 
                    ItemsSource="{Binding Registers}" 
                    AutoGenerateColumns="False" 
                    IsReadOnly="True"
                    GridLinesVisibility="All"
                    HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="寄存器地址" Binding="{Binding Address}" Width="0.3*"/>
                        <DataGridTextColumn Header="当前值" Binding="{Binding Value}" Width="0.3*"/>
                        <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="0.4*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <!-- 通信日志 -->
            <GroupBox Grid.Row="1" Header="通信日志" Height="150" Margin="0,10,0,0">
                <DataGrid 
                    ItemsSource="{Binding CommunicationLogs}" 
                    AutoGenerateColumns="False" 
                    IsReadOnly="True"
                    GridLinesVisibility="Horizontal"
                    HeadersVisibility="Column"
                    CanUserSortColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="时间" Binding="{Binding Timestamp, StringFormat='HH:mm:ss'}" Width="0.2*"/>
                        <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="0.15*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{Binding Type, Converter={converters:TypeToColorConverter}}"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="消息" Binding="{Binding Message}" Width="0.65*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>

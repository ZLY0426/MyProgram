<UserControl x:Class="MyProgram.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- 标题 -->
            <RowDefinition Height="*"/>
            <!-- 表格 -->
            <RowDefinition Height="Auto"/>
            <!-- 分页栏 -->
        </Grid.RowDefinitions>

        <!-- 1. 标题 -->
        <TextBlock 
            Grid.Row="0"
            Text="用户行为记录" 
            FontSize="24" 
            FontWeight="Bold" 
            Margin="0,0,0,20"/>

        <!-- 2. 数据表格 -->
        <DataGrid 
            Grid.Row="1"
            ItemsSource="{Binding Logs}" 
            AutoGenerateColumns="False" 
            IsReadOnly="True" 
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            GridLinesVisibility="Horizontal"
            HeadersVisibility="Column"
            RowHeight="30">

            <DataGrid.Columns>
                <!-- 用户ID: 权重 0.2 -->
                <DataGridTextColumn 
                    Header="用户ID" 
                    Binding="{Binding UserId}" 
                    Width="0.2*"
                    ElementStyle="{StaticResource DataGridCellCenterStyle}"/>

                <!-- 用户名: 权重 0.1 -->
                <DataGridTextColumn 
                    Header="用户名" 
                    Binding="{Binding Username}" 
                    Width="0.1*"/>

                <!-- 用户行为: 权重 0.4 -->
                <DataGridTextColumn 
                    Header="用户行为" 
                    Binding="{Binding Action}" 
                    Width="0.4*"/>

                <!-- 时间戳: 权重 0.2 -->
                <DataGridTextColumn 
                    Header="时间戳" 
                    Binding="{Binding Timestamp, StringFormat='yyyy-MM-dd HH:mm:ss'}" 
                    Width="0.2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 3. 底部分页栏 -->
        <Border Grid.Row="2" Margin="0,20,0,0" Padding="15,10" Background="#F8F8F8" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 上一页按钮 -->
                <Button 
                    Grid.Column="0"
                    Content="上一页" 
                    Command="{Binding PrevPageCommand}"
                    IsEnabled="{Binding PrevPageCommand.IsActive, FallbackValue=True}"
                    Width="90" Height="32"
                    Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>

                <!-- 页码显示 (居中) -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="当前页: " VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentPage}" FontWeight="Bold" VerticalAlignment="Center" FontSize="14"/>
                    <TextBlock Text=" / " VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalPages}" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- 下一页按钮 -->
                <Button 
                    Grid.Column="2"
                    Content="下一页" 
                    Command="{Binding NextPageCommand}"
                    Width="90" Height="32"
                    Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>
            </Grid>
        </Border>

        <!-- 页面加载事件触发 -->
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="Loaded">
                <i:InvokeCommandAction Command="{Binding LoadedCommand}"/>
            </i:EventTrigger>
        </i:Interaction.Triggers>
    </Grid>
</UserControl>

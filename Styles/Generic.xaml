<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:MyProgram.CustomControl">
    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    <!-- 导航按钮样式 -->
    <Style x:Key="CarouselNavButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Background" Value="#80FFFFFF"/>
        <Setter Property="Foreground" Value="#333333"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="16" BorderThickness="0">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#BBFFFFFF"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- 轮播控件样式 -->
    <Style TargetType="{x:Type local:CarouselView}">
        <Setter Property="TransitionDuration" Value="0.3"/>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>

        <!-- 每个项的宽度 = 视口宽度，确保一次只显示一项 -->
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Width" 
                            Value="{Binding ViewportWidth, RelativeSource={RelativeSource AncestorType=ScrollViewer}}"/>
                </Style>
            </Setter.Value>
        </Setter>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CarouselView}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <DockPanel>
                                <!-- 左导航按钮，可见性绑定到 ShowNavigationButtons -->
                                <Button x:Name="PART_PreviousButton"
                                        Content="◀"
                                        Style="{StaticResource CarouselNavButtonStyle}"
                                        DockPanel.Dock="Left"
                                        VerticalAlignment="Center"
                                        Visibility="{Binding ShowNavigationButtons, 
                                                     RelativeSource={RelativeSource TemplatedParent},
                                                     Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                <!-- 右导航按钮，同样绑定可见性 -->
                                <Button x:Name="PART_NextButton"
                                        Content="▶"
                                        Style="{StaticResource CarouselNavButtonStyle}"
                                        DockPanel.Dock="Right"
                                        VerticalAlignment="Center"
                                        Visibility="{Binding ShowNavigationButtons, 
                                                     RelativeSource={RelativeSource TemplatedParent},
                                                     Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                <!-- 主滚动区域：不设宽度，自动填充剩余空间 -->
                                <ScrollViewer x:Name="PART_ScrollViewer"
                                              HorizontalScrollBarVisibility="Hidden"
                                              VerticalScrollBarVisibility="Disabled"
                                              CanContentScroll="False"
                                              ClipToBounds="True">
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </DockPanel>

                            <!-- 指示器浮在底部中央 -->
                            <ItemsControl x:Name="PART_IndicatorPanel"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Bottom"
                                          Margin="0,0,0,15">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Ellipse Width="8" Height="8" Margin="3">
                                            <Ellipse.Style>
                                                <Style TargetType="Ellipse">
                                                    <Setter Property="Fill" 
                                                            Value="{Binding UnselectedIndicatorBrush, 
                                                                  RelativeSource={RelativeSource AncestorType=local:CarouselView}}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                            <Setter Property="Fill" 
                                                                    Value="{Binding SelectedIndicatorBrush, 
                                                                          RelativeSource={RelativeSource AncestorType=local:CarouselView}}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Ellipse.Style>
                                        </Ellipse>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 为 AnimatedContentControl 提供默认样式 -->
    <Style TargetType="{x:Type local:AnimatedContentControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:AnimatedContentControl}">
                    <Grid Background="{TemplateBinding Background}">
                        <!-- 旧内容层（先显示，后滑出） -->
                        <ContentPresenter 
                            x:Name="OldContentPresenter"
                            Content="{TemplateBinding Content}">
                            <ContentPresenter.RenderTransform>
                                <TranslateTransform X="0" Y="0"/>
                            </ContentPresenter.RenderTransform>
                        </ContentPresenter>

                        <!-- 新内容层（后显示，滑入） -->
                        <ContentPresenter 
                            x:Name="NewContentPresenter">
                            <ContentPresenter.RenderTransform>
                                <TranslateTransform X="0" Y="0"/>
                            </ContentPresenter.RenderTransform>
                        </ContentPresenter>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
